Add a `claimed: N` summary line to the `ainarres status` output.

- In the pure `formatStatus()` function in bin/ainarres.mjs, after the blocked
  section (the "blocked: N" line and its task-id list) and before the "abandoned"
  section, print one line `  claimed: N` where N is the number of board rows whose
  `claimed_by` is non-null (a task currently held by an agent). Always emit it, even
  when N is 0.
- Touch only bin/ainarres.mjs (the formatStatus function) and test/status-format.test.ts.
  No CLI flags, no network, no database — formatStatus takes already-fetched rows.
- Extend test/status-format.test.ts with a NEW assertion: given board rows where some
  have a non-null claimed_by, the output contains the correct `claimed: N` line. The
  assertion must FAIL on current main (the line doesn't exist yet) and pass after the
  change.

Per-task validate (substrate-free, discriminating — fails on main, passes after the
change): `npx vitest run test/status-format.test.ts`. Do not run the full suite.
